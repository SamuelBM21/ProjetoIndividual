#ifndef BIB_HPP
#define BIB_HPP

#include <cstdint>

// Calcula o fatorial de n (n >= 0).
// Retorna 0 se ocorrer overflow (ou seja, valor não caber em unsigned long long).
unsigned long long factorial(unsigned int n);

#endif // BIB_HPP
